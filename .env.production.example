# Production E2E Test Configuration
# Copy to .env.production and use when running smoke tests against live deployment

# Backend API URL (Railway production)
BACKEND_URL=https://tross-api-production.up.railway.app

# Note: E2E tests use dev tokens for authentication
# Production smoke tests should be run manually with real Auth0 login
# See docs/TESTING.md for smoke test checklist
