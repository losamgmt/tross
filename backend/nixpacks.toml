# Railway Nixpacks Configuration
# Forces Node 22 LTS and configures proper build

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --prefer-offline --no-audit --legacy-peer-deps"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
